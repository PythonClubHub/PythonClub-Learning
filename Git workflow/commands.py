# git clone                 - Clone repository
# git log                   - Show log commits
# git pull                  - Fetches from repository
# git push                  - Upload commits to remote repository
# git add                   - Stage files
# git commit                - Save staged files
# git switch                - Change branch
# git checkout              - General operation (see git checkout --help)
# git status                - Show working tree status 
# git merge                 - Join two branches